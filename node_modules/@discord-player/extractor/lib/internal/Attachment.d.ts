/// <reference types="node" />
import http from 'http';
export declare function Attachment(url: string): Promise<AttachmentRaw>;
export interface AttachmentRaw {
    stream: http.IncomingMessage;
    url: string;
    format: string;
}
