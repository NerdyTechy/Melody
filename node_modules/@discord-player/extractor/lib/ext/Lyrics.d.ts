import { Client as GeniusClient } from 'genius-lyrics';
export declare function init(apiKey?: string, force?: boolean): {
    search: typeof search;
    client: GeniusClient;
};
declare function search(query: string): Promise<LyricsData>;
export interface LyricsData {
    title: string;
    id: number;
    thumbnail: string;
    image: string;
    url: string;
    artist: {
        name: string;
        id: number;
        url: string;
        image: string;
    };
    lyrics?: string;
}
export {};
